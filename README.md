
## The command creates directory why && means run cd if mkdir succeed
mkdir Project-setup && cd Project-setup

## This command means initialise Project-setup into git repository so it will have Initialized empty Git repository
git init

## This command creates empty index.html file
touch index-html

## This command shows all the visible files and folders inside the Project-setup, which in this case is index.html
Is

## This command will open index.html in vi text editor for you to write on it
vi index.html
